---
id: polish
title: Polish and export
mode: wait
needs: [build-draft]
outputs: [final.mp4]
hint: "Open the draft named in in/build-draft/draft.json, do the human pass, export here as final.mp4"
---

The human pass: caption styling the generator cannot set, punch-ins per
beat, colour, music level, the end card. Then export vertical and drop the
file into this node's `out/` as `final.mp4`.

Before editing a generated draft, turn snapping off — a ripple slides later
clips and desyncs the two layers.
